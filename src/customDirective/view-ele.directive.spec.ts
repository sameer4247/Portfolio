import { ViewEleDirective } from './view-ele.directive';

describe('ViewEleDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewEleDirective();
    expect(directive).toBeTruthy();
  });
});
